# As a system administrator, you are tasked to configure the webserver1 container that is based on the
# http24 container image to start at system boot. You must also mount the /app-artifacts directory
# for the web server content and map the 8080 port from the local machine to the container.
# Configure the container to start and stop with systemctl commands.

# container name : webserver1
# container image : http24
# persistent dir: /app-artifacts
# port: 8080
